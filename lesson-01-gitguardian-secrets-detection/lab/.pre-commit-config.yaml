# Pre-commit configuration for GitGuardian secrets detection
# Install: pip install pre-commit && pre-commit install
repos:
  - repo: https://github.com/gitguardian/ggshield
    rev: v1.27.0
    hooks:
      - id: ggshield
        language_version: python3
        stages: [commit]
